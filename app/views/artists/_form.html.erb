<div class="container">
<%= form_for @artist do |f| %>
<%= f.label "Artist Name:" %> <br>
<%= f.text_field :name %>
<br><br>


<%= f.submit "#{button_label}", class: 'btn btn-small orange white-text' %>

<% end %>
</div>
