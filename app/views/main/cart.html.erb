
<div class = "container">

<h3><%= @current_user.username%></h3>
<% @current_user.albums.each do |a|%>
<p><b><%= a.title %></b> <%=a.artist.name%> <i>$<%= a.price %></i></p>

<%= button_to "buy", "#", :method => "delete", class: 'btn btn-small light-brown white-text'   %><%= button_to "remove", "/main/#{a.id}", :method => "patch", class: 'btn btn-small red white-text'   %>

<br>
<%end%>


</div>

